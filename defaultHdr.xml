<?xml version="1.0" encoding="UTF-8"?>
<teiHeader xmlns="http://www.tei-c.org/ns/1.0">
 <fileDesc>
  <titleStmt>
   <title>A TEI Bibliography</title>
  </titleStmt>
  <publicationStmt><p>Unpublished working file</p></publicationStmt>
  <sourceDesc><p>No source</p></sourceDesc>
 </fileDesc><encodingDesc>
 <listPrefixDef>
  <prefixDef ident="bl" matchPattern="(.+)"
   replacementPattern="http://access.bl.uk/item/viewer/$1">
   <p> Private URIs using the <code>bl</code> are British Library ark identifiers </p>
  </prefixDef>
  <prefixDef ident="ht" matchPattern="([A-Za-z0-9]+)"
   replacementPattern="https://babel.hathitrust.org/cgi/pt?id=$1">
   <p> Private URIs using the <code>ht</code> are Hathi Trust identifiers </p>
  </prefixDef>
  <prefixDef ident="ia" matchPattern="([A-Za-z0-9]+)"
   replacementPattern="https://archive.org/details/$1">
   <p> Private URIs using the <code>ia</code> prefix are pointers to one or more pages on
    Internet Archive. </p>
  </prefixDef>
  <prefixDef ident="gb" matchPattern="([A-Za-z0-9]+)"
   replacementPattern="https://books.google.co.uk/books?id=$1">
   <p> Private URIs using the <code>gb</code> prefix are pointers to one or more pages on Google
    Books </p>
  </prefixDef>
  <prefixDef ident="vw" matchPattern="(VAB[0-9]*)"
   replacementPattern="http://purl.dlib.indiana.edu/iudl/vwwp/encodedtext/$1">
   <p> Private URIs using the <code>vw</code> prefix can be expanded to form URIs which retrieve
    the relevant TEI XML file from the Victorian Women Writers Project </p>
  </prefixDef>
  <prefixDef ident="pg" matchPattern="([a-z]+[a-z0-9]*)"
   replacementPattern="http://www.gutenberg.org/ebooks/$1">
   <p> Private URIs using the <code>pg</code> prefix can be expanded to form URIs which point to
    the relevant download page at Project Gutenberg </p>
  </prefixDef>
  <prefixDef ident="ad" matchPattern="([a-z]+[a-z0-9]*)"
   replacementPattern="https://ebooks.adelaide.edu.au/$1">
   <p> Private URIs using the <code>ad</code> prefix can be expanded to form URIs which point to
    the relevant download page at the ebooks@Adelaide site </p>
  </prefixDef>
 </listPrefixDef></encodingDesc>
</teiHeader>
